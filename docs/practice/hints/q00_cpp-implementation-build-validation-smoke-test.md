# Q0 Hint: C++ Implementation: Build Validation Smoke Test

**Section:** Month 0: Core Systems Programming, Resource Stability, & DS/A (C++)

## Hint

Use a header-only `constexpr` function and an assert-based test executable. Avoid third-party test frameworks for this smoke test so the build only depends on the compiler, CMake, and CTest.
